---
import CalIframe from '@/components/cal-iframe.astro';
import ContactForm from '@/components/contact-form.astro';
import Tabs from '@/components/tabs.astro';
---

<div class="mx-auto max-w-xl">
  <Tabs
    tabs={[
      { id: 'cal', label: 'Réserver un appel', buttonClassName: 'rounded-tl-lg' },
      { id: 'contact', label: 'Nous contacter', buttonClassName: 'rounded-tr-lg' },
    ]}
    buttonClassName="border-b border-gray-100 border-b-4 w-full data-active:bg-white data-inactive:bg-gray-100 data-active:border-gray-100 data-inactive:border-gray-100 data-inactive:hover:border-gray-100 data-inactive:hover:text-gray-700 data-inactive:text-gray-400 uppercase font-bold"
    tabContentClassName="mt-0 p-4 rounded-b-lg"
  >
    <div data-tab-content="contact">
      <ContactForm light />
    </div>
    <div data-tab-content="cal" class="overflow-hidden">
      <CalIframe />
    </div>
  </Tabs>
</div>

<script type="text/javascript">
  (function (C, A, L) {
    let p = function (a, ar) {
      a.q.push(ar);
    };
    let d = C.document;
    C.Cal =
      C.Cal ||
      function () {
        let cal = C.Cal;
        let ar = arguments;
        if (!cal.loaded) {
          cal.ns = {};
          cal.q = cal.q || [];
          d.head.appendChild(d.createElement('script')).src = A;
          cal.loaded = true;
        }
        if (ar[0] === L) {
          const api = function () {
            p(api, arguments);
          };
          const namespace = ar[1];
          api.q = api.q || [];
          if (typeof namespace === 'string') {
            cal.ns[namespace] = cal.ns[namespace] || api;
            p(cal.ns[namespace], ar);
            p(cal, ['initNamespace', namespace]);
          } else p(cal, ar);
          return;
        }
        p(cal, ar);
      };
  })(window, 'https://app.cal.com/embed/embed.js', 'init');
  Cal('init', 'etude-de-projet', { origin: 'https://cal.com' });

  // Important: Please add the following attributes to the element that should trigger the calendar to open upon clicking.
  // `data-cal-link="anthony-hexium/etude-de-projet"`
  // data-cal-namespace="etude-de-projet"
  // `data-cal-config='{"layout":"month_view"}'`

  Cal.ns['etude-de-projet']('ui', { hideEventTypeDetails: false, layout: 'month_view' });
</script>
<!-- Cal element-click embed code ends -->
